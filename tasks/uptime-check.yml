# local usage:
#
#   fly execute -c tasks/uptime-check.yml -i scripts=.
#
platform: linux
image_resource:
  type: docker-image
  source:
    repository: ubuntu
    tag: '14.04'
inputs:
- name: scripts
run:
  path: ./scripts/tasks/uptime-check.sh
