# local usage:
#
#   # run fetch-project-data task first
#   mkdir -p tmp
#   fly execute -t lite -c tasks/uptime-check/task.yml -i scripts=. -i projects-json=tmp
#
platform: linux
image_resource:
  type: docker-image
  source:
    repository: node
    tag: '5.6-slim'
inputs:
- name: scripts
- name: projects-json
run:
  path: ./scripts/tasks/uptime-check/task.sh
